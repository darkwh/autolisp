1.将横向贯穿的所有中心线放到"center_line"图层中(图层名称可以自定义，对应的需要修改脚本，确保中心线在单独一个图层即可)
2.将所有图块中需要算交点的线，放到"スタッドdf"图层中(图层名称可以自定义，对应需要修改脚本,确保所有图块中的需要算交点的线在同一图层即可)
3.脚本生成标注之前，没有对已经存在的标注删除，如果之前已经有标注，需要提前先手动删除
4.脚本只计算了交点，生成标注，对于中心线两端的端点到最近的图块或图元交点未做实现，需要手动标注